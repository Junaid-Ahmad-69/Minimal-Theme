{
  "name": "templete_1",
  "version": "1.0.0",
  "description": "templet_1_with_scss",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --update --style=expanded scss:css",
    "sass-prod": "sass --no-source-map --style=compressed scss:css"
  },
  "author": "Junaid Ahmad",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.49.9"
  }
}
